# agents

Drop one `<slug>.md` per agent. Frontmatter shape: see `_TEMPLATE.md`. The MCP server reads the frontmatter and applies `reads.files` whitelist + `reads.filter.context_in` filter on every read.
