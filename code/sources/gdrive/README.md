# gdrive source

Resolver key: `folder_path` (glob match against `_contexts.yaml`). Window: work=9d, personal=730d.

Drops paths under `Trash/`, `Templates/`, or filenames matching `template_*`.

Stub: `NANOBRAIN_GDRIVE_STUB=/path/to/docs.json`.
